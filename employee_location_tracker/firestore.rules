rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo rules for this internal office tracker app.
    // Restrict to authenticated users before production rollout.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
